<UserControl x:Class="Unibox.Views.EditGameNotesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Unibox.Views"
             xmlns:vm="clr-namespace:Unibox.ViewModels"
             xmlns:properties="clr-namespace:Unibox.Properties"
            xmlns:icon="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:EditGameNotesVM, IsDesignTimeCreatable=True}"
             d:DesignHeight="321" d:DesignWidth="578" d:Foreground="#c0c0c0">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Dictionaries/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Edit Game Notes" DockPanel.Dock="Top" Style ="{DynamicResource PageHeader}" Margin="5" />
        <Grid Grid.Row="1" Margin="5">
            <TextBox Text="{Binding NotesText}" Padding="15" AcceptsReturn="True" VerticalContentAlignment="Top"
                     TextWrapping="Wrap"></TextBox>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Paste" Margin="5" MinWidth="100" Command="{Binding PasteCommand}"
              Padding="{Binding Source={x:Static properties:Settings.Default},Path=AppButtonPadding}" />

            <Button Content="Save" Margin="5" MinWidth="100" Command="{Binding SaveNotesCommand}"
                    IsEnabled="{Binding IsValid, Mode=OneWay, ValidatesOnDataErrors=True}"
                    Padding="{Binding Source={x:Static properties:Settings.Default},Path=AppButtonPadding}" />

            <Button Content="Cancel" Margin="5" MinWidth="100" Command="{Binding GoBackCommand}"
                    Padding="{Binding Source={x:Static properties:Settings.Default},Path=AppButtonPadding}" />
        </StackPanel>
    </Grid>
</UserControl>