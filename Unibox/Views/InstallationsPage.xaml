<UserControl x:Class="Unibox.Views.InstallationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Unibox.Views"
             xmlns:vm="clr-namespace:Unibox.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:InstallationsVM, IsDesignTimeCreatable=True}"
             d:DesignHeight="243" d:DesignWidth="551">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Dictionaries/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="3" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- INSTALLATIONS LIST -->
        <DockPanel Grid.Row="0" Margin="2">
            <TextBlock Text="Installations" FontWeight="Bold" DockPanel.Dock="Top" />
            <StackPanel DockPanel.Dock="Right" Orientation="Vertical" HorizontalAlignment="Right">
                <Button Content="Add" Command="{Binding AddNewInstallationCommand}"  Margin="2" />
                <Button Content="Delete" Command="{Binding DeleteInstallationCommand}" Margin="2" />
                <Button Content="Edit" Command="{Binding EditInstallationCommand}" Margin="2" />
            </StackPanel>
            <Border BorderBrush="gray" BorderThickness="1" Margin="1">
                <ListView ItemsSource="{Binding Installations}" SelectedItem="{Binding SelectedInstallation}"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Path" Width="auto" DisplayMemberBinding="{Binding InstallationPath}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </DockPanel>

        <GridSplitter Grid.Row="1"  HorizontalAlignment="Stretch"></GridSplitter>

        <DockPanel Grid.Row="2" Margin="2">
            <TextBlock Text="Platforms" FontWeight="Bold" DockPanel.Dock="Top" />
            <StackPanel DockPanel.Dock="Right" Orientation="Vertical" HorizontalAlignment="Right">
                <Button Content="Update" Command="{Binding AddNewInstallationCommand}"  Margin="2" />
                <Button Content="Edit" Command="{Binding DeleteInstallationCommand}" Margin="2" />
            </StackPanel>
            <Border BorderBrush="gray" BorderThickness="1" Margin="2">
                <ListView ItemsSource="{Binding Installations}" SelectedItem="{Binding SelectedInstallation}"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Path" Width="auto" DisplayMemberBinding="{Binding InstallationPath}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>