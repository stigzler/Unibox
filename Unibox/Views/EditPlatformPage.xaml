<UserControl x:Class="Unibox.Views.EditPlatformPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Unibox.Views"
             xmlns:vm="clr-namespace:Unibox.ViewModels"
             xmlns:properties="clr-namespace:Unibox.Properties"
            xmlns:icon="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:EditPlatformVM, IsDesignTimeCreatable=True}"
             d:DesignHeight="321" d:DesignWidth="578" d:Foreground="#c0c0c0">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Dictionaries/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding HeaderText}" DockPanel.Dock="Top" Style ="{DynamicResource PageHeader}" />
        <Grid Grid.Row="1" Margin="5">
            <GroupBox Grid.Row="0" Header="Tools" VerticalAlignment="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock  Grid.Column="0" Grid.Row="0" Text="Application" VerticalAlignment="Center" Margin="5"
                                ToolTip="Path to any 3rd party applications linked this platform (e.g. rom managers etc)" />
                    <TextBox x:Name="ApplicationTb" Grid.Column="1" Grid.Row="0" Margin="5"
                            Text="{Binding Platform.ApplicationPath}" />
                    <Button Grid.Column="2" Grid.Row="0"  Margin="2" Command="{Binding ClearAppPathCommand}"
                            Padding="{Binding Source={x:Static properties:Settings.Default},Path=AppButtonPadding}"
                            Width="{Binding ActualWidth, ElementName=ApplicationBrowseBT}">
                        <icon:FluentIcon Icon="Eraser" FontSize="32" HorizontalAlignment="Center" MinWidth="32" MinHeight="32" />
                    </Button>
                    <Button x:Name="ApplicationBrowseBT" Grid.Column="3" Grid.Row="0"  Margin="2" Command="{Binding GetAppPathCommand}"
                            Padding="{Binding Source={x:Static properties:Settings.Default},Path=AppButtonPadding}">
                        <icon:FluentIcon Icon="FolderOpen" FontSize="32" HorizontalAlignment="Center" MinWidth="32" MinHeight="32" />
                    </Button>

                    <TextBlock  Grid.Column="0" Grid.Row="1" Text="URL" VerticalAlignment="Center" Margin="5"
                                           ToolTip="An associated URL for the platform (roms, emulator details etc)" />
                    <TextBox Grid.Column="1" Grid.Row="1" Margin="5" Grid.ColumnSpan="3"
                            Text="{Binding Platform.Url}" />

                    <TextBlock  Grid.Column="0" Grid.Row="2" Text="Notes" VerticalAlignment="Top" Margin="5"
                                           ToolTip="Any notes for the Platform (can be multi-line)" />
                    <TextBox Grid.Column="1" Grid.Row="2" Margin="5" Grid.ColumnSpan="3" AcceptsReturn="True" TextWrapping="Wrap"
                            Text="{Binding Platform.Notes}" />
                </Grid>
            </GroupBox>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Margin="5" MinWidth="100" Command="{Binding SavePlatformCommand}"
                    IsEnabled="{Binding IsValid, Mode=OneWay, ValidatesOnDataErrors=True}"
                    Padding="{Binding Source={x:Static properties:Settings.Default},Path=AppButtonPadding}" />

            <Button Content="Back" Margin="5" MinWidth="100" Command="{Binding NavigateToInstallationsCommand}"
                    Padding="{Binding Source={x:Static properties:Settings.Default},Path=AppButtonPadding}" />
        </StackPanel>
    </Grid>
</UserControl>