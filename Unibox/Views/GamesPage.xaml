<UserControl x:Class="Unibox.Views.GamesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Unibox.Views"
             xmlns:vm="clr-namespace:Unibox.ViewModels"
             xmlns:converters="clr-namespace:Unibox.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:GamesVM, IsDesignTimeCreatable=True}" d:Height="139" d:Width="574"
             Cursor="{Binding Cursor}">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToInverseVisibilityConverter x:Key="BoolToInverseVisibilityConverter" />
    </UserControl.Resources>

    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Height="auto" Margin="4">
            <Image Source="/Resources/Images/LaunchboxIcon.png" Width="32" />
            <ComboBox x:Name="InstallationsCB" MinWidth="200" ItemsSource="{Binding InstallationsView}"
                      DisplayMemberPath="Name" SelectedItem="{Binding SelectedInstallation}" />
            <StackPanel Orientation="Horizontal" Visibility="{Binding InstallationAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
                <Image Source="/Resources/Images/Platform.png" Width="32" />
                <ComboBox MinWidth="200" ItemsSource="{Binding PlatformsView}" DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedPlatform}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="left" Margin="4,0,0,0"
                    Visibility="{Binding InstallationAvailable, Converter={StaticResource BoolToInverseVisibilityConverter}}">
                <Image Source="/Resources/Images/minus-circle.png" Height="24" />
                <TextBlock Text="Installation Unreachable (is it online?)" VerticalAlignment="Center" Margin="5" />
            </StackPanel>
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="4">
            <Button Content="Add Game" />
        </StackPanel>

        <StackPanel Visibility="{Binding InstallationAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
            <!-- Games Grid and Search -->
            <DockPanel DockPanel.Dock="Top">
                <TextBlock DockPanel.Dock="Left" Text="Search: " FontWeight="Bold" Margin="4" />
                <TextBox Text="Games" FontWeight="Bold" Margin="4" />
            </DockPanel>
            <ListView></ListView>
        </StackPanel>
    </DockPanel>
</UserControl>